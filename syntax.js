var x;
var y;

x = 5;
y = 10;

console.log(x)
console.log(y)

//JavaScript syntax have two type of valus;
//1. fixed values; it's calls to (literals);
//2. varible values; it's calls to (variables)


//expression;
//expression are combination of values, variables and operators;

//statement;
//statement are combination of values, operators, expressions, keywords and comments;