const arr = [45,54,43,54,43,54];
const result = arr.slice(-4, -1) //two parameter.
// console.log(result);


//splice:
const spliceResult = arr.splice(1,2,1,2,3,4,5,6,7,8); // three parameter. 1: when i take 2: who may will deltete 3: still there they will added in this array.
console.log(arr);